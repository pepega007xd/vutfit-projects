"use client";

import { createTheme } from "@mui/material";

// theme declaration for MaterialUI components,
// must be in a client component
export const darkTheme = createTheme({
  palette: {
    mode: 'dark',
  },
});
